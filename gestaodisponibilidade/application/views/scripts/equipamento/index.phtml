
<table class="style2"> 
    <thead> 
        <tr> 
            <th>Identificador</th>
            <th>Descrição</th>
            <th>Ação</th>
        </tr> 
    </thead> 
    <? foreach ($this->listaEquipamentos as $item): ?> 
        <tbody> 
            <tr> 
                <td><?= $item->getId_equipamento() ?></td> 
                <td><?= $item->getDescricao() ?></td>
                <td>
                    <a href="<?= $this->baseUrl('/equipamento/editar-equipamento/') ?><?= $item->getId_equipamento() ?>">Editar</a>
                </td>
            </tr>
        </tbody>
    <? endforeach; ?>
</table>